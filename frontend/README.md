# AI Sakhi Frontend

This is the canonical Next.js frontend for AI Sakhi.

## Run

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` in `frontend/.env.local` if your backend is not running at `http://localhost:8000`.
