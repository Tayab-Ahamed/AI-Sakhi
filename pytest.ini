[pytest]
testpaths = backend/tests tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --tb=short --strict-markers
asyncio_mode = strict
markers =
    integration: requires the full application stack (FastAPI, Chroma)
